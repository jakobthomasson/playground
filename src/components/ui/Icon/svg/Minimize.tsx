import React, { SFC, SVGProps } from 'react';

const Minimize: SFC<SVGProps<SVGSVGElement>> = props => (
  <svg viewBox="0 0 42 42">
    <rect y="20" width="42" height="2" />
  </svg>
);

export default Minimize;
