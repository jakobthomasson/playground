import { createSelector } from 'reselect';
import Types from 'Types';

// export const byId = (state: Types.RootState) => state.documentDomain.byId;

// export const orderedList = createSelector(
//   byId,
//   allIds,
//   (byId, allIds) => allIds.map(id => byId[id]),
// );

export const byId = (state: Types.RootState) => 'undefined';
